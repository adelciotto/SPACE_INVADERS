#ifndef _SHARED_H_
#define _SHARED_H_

#include <assert.h>
#include <stdint.h>
#include <stdlib.h>

#include "lib/adc_log.h"

#define MAX(A, B) (((A) >= (B)) ? (A) : (B))
#define MIN(A, B) (((A) <= (B)) ? (A) : (B))

#endif // _SHARED_H_
